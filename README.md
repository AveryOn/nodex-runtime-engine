# Nodex Runtime Engine

#### A low-level Node.js runtime laboratory focused on event loop behavior, async execution, streams, buffers, worker threads, clustering, memory leaks, profiling, async hooks, and performance benchmarking through practical backend-oriented experiments.